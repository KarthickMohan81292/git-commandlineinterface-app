package com.git.commandlineinterfaceapp;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Runs the Spring boot application.
 */
@SpringBootApplication
public class CommandLineInterfaceApplication {

	/**
	 * Main method which starts the sprint boot application.
	 * @param args - Args
	 */
	public static void main(String[] args) {
		SpringApplication.run(CommandLineInterfaceApplication.class, args);
	}

}
